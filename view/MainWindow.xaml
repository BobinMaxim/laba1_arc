<Window x:Class="view.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:view"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <ListView ItemsSource="{Binding IsPrisoners}" DisplayMemberPath="Name"  HorizontalAlignment="Left" Height="100" Margin="180,102,0,0" VerticalAlignment="Top" Width="100">
        </ListView>
        <ListView ItemsSource="{Binding IsnotPrisoners}" DisplayMemberPath="Name"  HorizontalAlignment="Left" Height="100" Margin="327,102,0,0" VerticalAlignment="Top" Width="100">
           
        </ListView>

    </Grid>
</Window>
